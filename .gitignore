# Environment & secrets
.env
*.pem
*.key

# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg

# Virtual environments
.venv/
venv/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Docker persistent data (local only)
pgdata/

# Test & coverage
.pytest_cache/
htmlcov/
.coverage
coverage.xml

# Alembic autogenerated (keep versions/, ignore temp)
*.pyc

# Data exports (generated at runtime)
data/*.pdf
data/*.png
data/*.csv

# Patch archives and DB dumps (contain personal data)
dist/
backups/
*.tar.gz
*.sql.gz

# AppleDouble-мусор macOS: ломает импорт ревизий alembic
._*

# Конфиг nginx на сервере общий для нескольких сайтов (lor-dok.ru,
# kod-03.ru, meduwiki.ru, n8n и другие) и живёт только там.
# В репозитории его быть не должно: деплой затирал бы соседей.
site/nginx.conf
